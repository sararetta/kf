import React from "react";
import { StyledHeader } from "./styles";
type Props = {};

const Header = (props: Props) => {
  return (
    <StyledHeader>
      <p>Groups/Slots</p>
    </StyledHeader>
  );
};

export default Header;
